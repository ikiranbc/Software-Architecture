# Docker Orchestration

The root-level `docker-compose.yml` is the runnable compose entrypoint for local development.

Run:

```bash
docker compose up --build
```

This `infra/docker` folder is kept to match architecture layout requirements.
